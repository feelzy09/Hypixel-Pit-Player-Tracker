# Hypixel-Pit-Player-Tracker
A discord bot that shows what players are online using the command /checkpit.

First download dependencies then run it using the command node index.js.

Then you should be all good to go.
